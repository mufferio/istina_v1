"""
Adapters to external input sources.

Purpose:
- Convert external formats (RSS/JSON APIs) into domain entities.
- Keep parsing / network boundaries here.

Example:
- rss_adapter.py reads RSS feeds and returns Article objects.
"""
